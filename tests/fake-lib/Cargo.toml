[package]
name = "fake-lib"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
usdt = { path = "../../usdt", features = ["asm"] }

[build-dependencies]
usdt = { path = "../../usdt", features = ["asm"] }
version_check = "0.9.4"
