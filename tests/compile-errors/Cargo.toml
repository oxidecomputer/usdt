[package]
name = "compile-errors"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
usdt = { path = "../../usdt" }
serde = "*"
serde_json = "*"

[dev-dependencies]
trybuild = "1.0.98"

[build-dependencies]
version_check = "0.9.5"
